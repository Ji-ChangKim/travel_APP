# travel_APP 프로젝트 개발 규칙 및 주의 사항

이 문서는 사용자가 지정한 프로젝트 개발 핵심 규칙을 정의합니다. 모든 코드 작성 및 git 작업 시 이 규칙을 엄격하게 준수합니다.

---

## 1. Git 푸쉬 / 커밋 메시지 규칙

- **형식**: `타이틀 : 설명`
- **지침**:
  - 변경 내용의 핵심을 명확한 타이틀로 요약하고, 콜론(`:`) 뒤에 세부적인 변경 이유 및 동작 내역을 세세하게 기술합니다.
  - 예시:
    - `feat: 여행 상세 5대 허브 및 도메인 모델 고도화 : 기획서 v0.3 기반 일정·지도·비용·체크리스트·멤버 5대 탭 구현 및 DAY 자동 생성 엔진 연동`
    - `docs: DB 및 도메인 객체 명세 관리 문서 추가 : 11개 테이블 및 도메인 객체 컬럼 속성 상세 정리`

---

## 2. DB 및 객체 명세 관리 문서 유지 규칙

- **문서 위치**: `docs/DB_DICTIONARY.txt`
- **지침**:
  - 데이터베이스 테이블 및 도메인 객체를 생성하거나 수정할 때, 반드시 해당 파일에 **DB명, 테이블명, 컬럼 속성, 타입, 제약조건, 설명**을 세세하게 기록하고 최신 상태로 유지합니다.

---

## 3. 코드 작성 규칙 (단일 명령 함수 및 한글 주석)

- **단일 명령 원칙**:
  - 함수는 반드시 **단 하나의 명령/책임(Single Responsibility)**만을 수행해야 합니다.
  - 복합적인 작업은 작은 단위의 단일 명령 함수들로 분리하여 호출합니다.
- **한글 주석 필수**:
  - 모든 함수 본문에는 단일 명령을 두고 동작을 상세히 설명하는 **한글 주석**을 필수로 작성합니다.

---

## 4. 검증 및 빌드 규칙

- 코드 수정 후 반드시 `npm run check` (TypeScript 엄격 모드, ESLint 린트, Prettier 포맷)를 100% 통과해야 합니다.
- Expo 패키지 추가 시 `npx expo install 패키지명`을 사용합니다.
